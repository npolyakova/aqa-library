import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import ru.library.Newspaper;
import java.time.Instant;

public class NewspaperTest {
    @Test
    public void shouldGetDate(){
        //не смог разобраться в каком формате ввести дату Instant
        //из-за чего не смог написать тест

        //Newspaper newspaper = new Newspaper("bookName", 1222343456678954);
        //Instant res = newspaper.getDate();
        //String res1 = newspaper.getName();

        //Assertions.assertEquals("bookName", res1);
        //Assertions.assertNotNull(res);
    }
}
